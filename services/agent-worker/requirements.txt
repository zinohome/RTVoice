# LiveKit Python SDK（rtc 客户端）
livekit==1.0.16
livekit-api==1.1.0

# VAD：直接 onnxruntime 加载 silero VAD ONNX 文件，
# 避开 silero-vad pip 包（它会拖 torch + CUDA toolkit ~12GB）
onnxruntime==1.20.1

# WebSocket 客户端（连 stt-server）
websockets==13.1

# OpenAI 兼容 API 客户端（连 llm-server / 未来的 vLLM）
openai==1.59.7

# HTTP 异步客户端（连 tts-server 的流式 chunked endpoint；OpenAI SDK 也用 httpx）
httpx==0.27.2

# Prometheus 指标（独立 :9100/metrics 端口）
prometheus-client==0.21.1

# 音频处理
numpy==2.2.1
