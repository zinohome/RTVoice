# LiveKit Python SDK（rtc 客户端）
livekit==1.0.16
livekit-api==1.1.0

# VAD：直接 onnxruntime 加载 silero VAD ONNX 文件，
# 避开 silero-vad pip 包（它会拖 torch + CUDA toolkit ~12GB）
onnxruntime==1.20.1

# 音频处理
numpy==2.2.1
