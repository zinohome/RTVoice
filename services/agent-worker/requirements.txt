# LiveKit Python SDK + 各客户端
# v0.6 引入 livekit-agents 后多个包需要协商版本，统一用 >= 约束
livekit>=1.0.16
livekit-api>=1.1.0

# VAD：直接 onnxruntime 加载 silero VAD ONNX 文件
onnxruntime>=1.20.1
numpy>=2.2.1

# WebSocket 客户端（连 stt-server）
websockets>=13.1

# OpenAI 兼容 API 客户端（连 llm-server / 未来的 vLLM）
openai>=1.59.7

# HTTP 异步客户端（连 tts-server）
httpx>=0.27.2

# Prometheus 指标
prometheus-client>=0.21.1

# ============================================================
# v0.6 (experimental) — livekit-agents 框架迁移
# 通过 AGENT_BACKEND=v06 启用；默认 v0.5.1 不会用到 framework runtime
# ============================================================
livekit-agents>=1.5.6,<2.0
livekit-plugins-openai>=1.5.6,<2.0
livekit-plugins-silero>=1.5.6,<2.0
