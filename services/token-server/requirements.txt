# 允许 patch 升级，避免镜像源缺少精确版本
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
livekit-api>=0.8,<0.9
pydantic>=2.10,<3

# 限流：每 IP/分钟阈值（防 API key 泄露后被刷）
slowapi==0.1.9

# Prometheus 指标（GET /metrics）
prometheus-client>=0.21,<0.22
prometheus-fastapi-instrumentator>=7.0,<8
